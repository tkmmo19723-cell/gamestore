# NTPGame — Premium Frontend
Website bán acc game + dịch vụ cày thuê, thiết kế dark neon, responsive mobile và có animation.
Upload toàn bộ file vào cùng thư mục GitHub Pages.
Mở `index.html` để chạy bản frontend. Supabase starter schema nằm trong `supabase.sql`.
